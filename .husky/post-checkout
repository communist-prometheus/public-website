BRANCH=$(git rev-parse --abbrev-ref HEAD)
git fetch content "$BRANCH" 2>/dev/null && git subtree pull --prefix=src/content content "$BRANCH" --squash -m "content: sync after checkout" 2>/dev/null || true
